# 本地运行需要把文件名改为env.ini
# debug release test
env_mode = debug
# 提供下载文件的存放路径(留空则默认程序执行的当前目录)
# 可相对路径，如：./download_lib | download_lib
# 可绝对路径，如：E:\download_lib | /tmp/download_lib
download_lib_path = download_lib

[server]
# http协议
protocol = http
# http端口号
http_port = 9801